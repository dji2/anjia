import { MianjixiaoPipe } from './mianjixiao.pipe';

describe('MianjixiaoPipe', () => {
  it('create an instance', () => {
    const pipe = new MianjixiaoPipe();
    expect(pipe).toBeTruthy();
  });
});
